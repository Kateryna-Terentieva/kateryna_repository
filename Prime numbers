showPrimes (20);

function showPrimes(limit){
for (let number = 2; number <= limit; number++)
    if (isPrime(nuber)) console.log(number); 
}


  fuction isPrime(number){
    let isPrime = true;
    for (let factor = 2; factor < number; factor ++){
        if (number % factor === 0 ){
            return false;

            return false;
        }
    }

    
}    
}
function isPrime(number {

})